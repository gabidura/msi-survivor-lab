# Quick GitHub upload notes

Suggested repository name: `msi-survivor-lab`

Suggested repository description:

> Python tools for primorial survivor envelopes, windowed survivor counts, layered residual-prime attrition, and localized prime-constraint experiments.

Suggested topics:

`number-theory`, `prime-numbers`, `finite-sieve`, `computational-number-theory`, `goldbach`, `python`, `mathematics`

First commands:

```bash
git init
git add .
git commit -m "Initial MSI Survivor Lab Python release"
git branch -M main
git remote add origin https://github.com/<your-user>/msi-survivor-lab.git
git push -u origin main
```

After upload, edit `CITATION.cff` and replace `https://github.com/USERNAME/msi-survivor-lab` with the real GitHub URL.
